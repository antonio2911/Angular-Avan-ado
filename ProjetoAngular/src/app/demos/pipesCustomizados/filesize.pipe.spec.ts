import { FileSizePipe } from './filesize.pipe';
import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('fileSizePipe', () => {
  describe('testeInsolado', () => {
    const pipe = new FileSizePipe();
    it('Deve converter bytes para MB');
  });
});
